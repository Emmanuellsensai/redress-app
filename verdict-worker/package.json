{
  "name": "redress-verdict-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local dev harness for the AI verdict engine (production runs as a Vercel serverless function at frontend/api/verdict.ts)",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "npx tsx src/dev-server.ts"
  },
  "dependencies": {
    "@redress/sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
